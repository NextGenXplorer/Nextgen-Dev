import 'package:flutter/material.dart';

class DeployScreen extends StatelessWidget {
  const DeployScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Deploy Screen')),
    );
  }
}
